<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorControlNormal">@color/white</item>
    </style>

    <style name="DialogTheme" parent="@android:style/Theme.Dialog">
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@color/white</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowAnimationStyle">@style/DialonAnimation</item>
    </style>

    <style name="DialonAnimation" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:windowEnterAnimation">@anim/fadin</item>
        <item name="android:windowExitAnimation">@anim/fadout</item>
    </style>

    <style name="poppins_light_text" parent="TextAppearance.AppCompat">
        <item name="android:padding">2dp</item>
        <item name="android:fontFamily">@font/poppins_light</item>
        <item name="android:textColor">@color/profile_light_text_color</item>
        <item name="android:textSize">12dp</item>
    </style>

    <style name="poppins_light_text.fees_text_light">
    <item name="android:layout_marginTop">10dp</item>
    <item name="android:textSize">10dp</item>
    </style>

    <style name="poppins_light_text.fees_text_light.fees_text_medium">
        <item name="android:textStyle">bold</item>
        <item name="android:fontFamily">@font/poppins_medium</item>
        <item name="android:layout_marginTop">0dp</item>
    </style>

    <style name="MineCustomTabText" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">12sp</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:fontFamily">@font/poppins_light</item>
    </style>

</resources>
